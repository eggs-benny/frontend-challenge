import { styled } from '@mui/material';

export const Watermark = styled('div')({
  color: 'grey',
  padding: '5px',
  fontSize: '10px',
  fontWeight: 'bolder',
  position: 'fixed',
  bottom: 0,
  left: 0
});
